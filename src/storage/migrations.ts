/**
 * Database migrations are handled by Dexie's versioning system in db.ts.
 * This file is reserved for data migrations that need to run after schema changes
 * (e.g., backfilling new fields on existing records).
 *
 * For v1, no data migrations are needed — the schema in db.ts is the initial version.
 */
